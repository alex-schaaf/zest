{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["STRAVA_CLIENT_ID", "STRAVA_CLIENT_SECRET", "JWT_SECRET"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
